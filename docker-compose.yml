services:
  app:
    build: .
    ports:
      - 8080:8080
    networks:
      - mynetwork

  nginx:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
    networks:
      - mynetwork

networks:
  mynetwork:
